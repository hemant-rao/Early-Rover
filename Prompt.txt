Replace the current flat 2D orbital "SOLAR 3D" circle in my Android app 
with a real, interactive 3D animation.

Tech approach:
- Use a WebView that loads a local HTML file with Three.js (r150+) via WebGL.
- Make the WebView background transparent so it blends with the app's dark theme.
- Enable hardware acceleration and JavaScript.

Scene requirements:
- A glowing 3D Sun at the center: emissive sphere with a soft bloom/glow 
  shader and subtle surface noise (animated fragment shader for "plasma" look).
- A camera slightly tilted so the orbit ring is seen at a 3/4 perspective 
  angle (not top-down), giving a true 3D feel.
- One elliptical orbit ring rendered as a thin glowing line. A small planet/
  marker travels along this orbit continuously and slowly.
- The marker's position on the orbit should represent the current time of 
  day (sunrise at one side, sunset at the other) — pass currentTime, 
  sunriseTime and sunsetTime from Android (Kotlin) to the WebView via 
  a JavaScript interface and update the position.
- Smooth 60fps rotation, gentle auto-orbit, and slow camera idle rotation.
- Add subtle floating particle stars in the background.
- Respond to touch: user can drag to rotate the camera (OrbitControls-style), 
  pinch to zoom slightly.

Performance:
- Cap pixel ratio to 2, dispose geometries properly, pause rendering 
  (cancelAnimationFrame) when the WebView/Activity is not visible to save battery.

Give me: the full HTML+Three.js file, the Kotlin code to set up the WebView, 
and the JavascriptInterface bridge to pass live sun data.