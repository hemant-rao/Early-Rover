<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- §806 — background location so the group/family live-share (EarlyRoverShareService)
         keeps reporting when the app is not in the foreground. Requested at runtime only
         when the user turns Sharing ON (Life360-style consent). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Wake and notification notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Alarm manager scheduling permissions -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground services and types -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- An alarm ringer is not media playback; use SPECIAL_USE so Android 14+ does not
         impose mediaPlayback/MediaSession restrictions (and Play does not flag the type). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!-- §750 — AdMob application id. This is Google's official TEST app id
             (safe to ship — it only ever serves test ads). Replace with the real
             AdMob app id recorded in the OdioBook admin (Branding > Other Apps >
             Solar AdMob App ID) before a production release. Required by the
             Mobile Ads SDK; without it the app crashes on ad init. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background active alarm ringing companion service.
             specialUse (not mediaPlayback): this FGS only rings/vibrates an alarm; it has no
             active MediaSession, so mediaPlayback would be the wrong type on Android 14+. -->
        <service
            android:name=".alarm.scheduling.AlarmService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Plays the alarm sound and vibration while a scheduled alarm is firing." />
        </service>

        <!-- Intelligent continuous travel tracking location service -->
        <service
            android:name=".alarm.location.TravelTrackingService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- §806 — Early Rover live group/family location sharing foreground service. -->
        <service
            android:name=".alarm.tracking.EarlyRoverShareService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- Alarm Manager broadcast receiver -->
        <receiver
            android:name=".alarm.scheduling.AlarmReceiver"
            android:exported="false" />

        <!-- Reschedule recovery receiver: re-arms / recalibrates alarms after the device
             reboots or its timezone changes.
             NOTE: ACTION_TIME_CHANGED is intentionally NOT listed here. It is a
             registration-only ("protected") broadcast that the OS will silently never
             deliver to a manifest receiver, so declaring it would give false confidence.
             A manual wall-clock change is handled by the dynamically-registered receiver
             in MainActivity while the app process is alive; out-of-process clock changes
             are corrected on the next app open / boot / timezone change (and, where added,
             a periodic WorkManager recalibration pass). -->
        <receiver
            android:name=".alarm.scheduling.RescheduleReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
